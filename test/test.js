var should = require('should');

describe('sample test', function() {
  it('1+1 is should be 2', function() {
    var r = 1+1;
    r.should.equal(2);
  });
  /*
  it('faild test', function() {
    'hoge'.should.equal('piyo');
  });
*/
});